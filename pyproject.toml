[tool.uv.workspace]
members = ["packages/*"]
# fb2skill-web is a Node/Vite package — npm-managed, not a uv member.
exclude = ["packages/fb2skill-web"]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "httpx>=0.27",
    "ruff>=0.5",
    "mypy>=1.10",
]
